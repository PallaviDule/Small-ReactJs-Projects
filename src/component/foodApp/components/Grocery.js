const Grocery = () => {
    return (
        <div>
            <h1> This is Grocery Page</h1>
            <h5>We are trying to load this component with lazy loading and confirming how bundling happens in this case.</h5>
        </div>
    )
};

export default Grocery;